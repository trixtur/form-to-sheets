{
  "name": "form-to-sheets",
  "private": true,
  "version": "1.0.0",
  "description": "Public form to Google Sheets triage workflow",
  "scripts": {
    "lint": "eslint .",
    "format:check": "prettier --check .",
    "check": "npm run lint && npm run format:check"
  },
  "devDependencies": {
    "eslint": "^9.20.1",
    "globals": "^15.14.0",
    "prettier": "^3.4.2"
  }
}
